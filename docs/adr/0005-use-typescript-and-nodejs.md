# Use TypeScript and Node.js

Daily Brief will use TypeScript on Node.js so the agent runtime, Operational CLI, Fetch Adapters, Discord Delivery, and shared domain types can stay in the same npm ecosystem as `earendil-works/pi`. This keeps integration with pi packages straightforward while supporting YAML, JSONL, Markdown, RSS, GitHub, YouTube, and Discord workflows with existing Node libraries.
